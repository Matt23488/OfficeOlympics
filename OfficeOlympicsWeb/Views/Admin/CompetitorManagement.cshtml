@model List<CompetitorViewModel>

@{
    ViewBag.Title = "Competitor Management";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h2>Competitor Management</h2>

<div class="container">
    <div class="row">
        <div class="col-lg-2">
            <a href="@Url.Action("AddCompetitor")" class="btn btn-success">
                <span class="glyphicon glyphicon-plus"></span>
                Add Competitor
            </a>
        </div>
    </div>
    <br />
    @if (Model.Count == 0)
    {
        <h4>No Competitors found.</h4>
    }
    else
    {
        @*<table class="table table-hover">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.First().FormattedName)</th>
                    </tr>
                </thead>
                <tbody>
                    @for (int i = 0; i < Model.Count; i++)
                    {
                        <tr class="competitor-item @(Model[i].IsActive ? "" : "bg-danger")">
                            <td>
                                <input type="hidden" class="hidden-competitor-id" value="@Model[i].CompetitorId" />
                                @Html.DisplayFor(model => model[i].FormattedName)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>*@
        <div class="row">
            <div class="col-lg-3 col-lg-push-4 col-md-3 col-md-push-4">
                <ul class="list-group">
                    @for (int i = 0; i < Model.Count; i++)
                    {
                        <li class="list-group-item list-group-item-action competitor-item text-center">
                            <input type="hidden" class="hidden-competitor-id" value="@Model[i].CompetitorId" />
                            @Html.DisplayFor(model => model[i].FormattedName)
                        </li>
                    }
                </ul>
            </div>
        </div>
    }
</div>