@model List<EventViewModel>

@if (Model.Any())
{
    <ul class="list-group">
        @for (int i = 0; i < Model.Count; i++)
        {
            <li class="list-group-item">@Html.DisplayFor(model => model[i].EventName)</li>
        }
    </ul>
}
else
{
    <em>No events to show.</em>
}